# Codex 작업 규칙

## 사용자와 대화

- 사용자는 비개발자다. 한국어로 짧고 쉽게, 한 번에 한 단계씩 설명한다.
- 명령 전에는 무엇을 하는지, 왜 지금 하는지, 어떤 결과가 정상인지 알린다.
- 삭제, 재시작, 설정 변경, DB 변경은 실행 전에 설명하고 승인을 받는다.
- 비밀번호, API 키, DB 접속정보, `.env` 내용은 출력하거나 커밋하지 않는다.

## 작업 시작

1. 기존 코드 구조와 구현 방식을 먼저 확인한다.
2. 추적 중인 변경을 확인해 다른 사람의 작업을 덮어쓰지 않는다.
3. `git fetch origin main` 후 최신 `main`을 기준으로 시작한다.
4. `main`에 직접 커밋하지 않고 `codex/<작업명>` 브랜치를 사용한다.
5. 진행 중인 원격 브랜치가 있으면 새 작업을 만들기 전에 이어받을지 확인한다.

## 수정과 검사

- 필요한 파일만 최소한으로 수정한다.
- 저장소 전체에 `prettier --write` 또는 `pnpm format`을 실행하지 않는다.
- 서식 검사는 수정한 파일만 먼저 확인한 뒤 적용한다.
- 파일 이름의 대소문자만 바꾸지 않는다.
- 권한 코드는 `published`, `pending`, `private` 상태를 각각 검토하고 테스트한다.
- DB에 들어갈 값은 실제 칼럼 크기를 먼저 확인한다.
- 기본 검증은 `pnpm check`, `pnpm test`, `pnpm build`다.
- 병합 뒤에는 실제 브라우저에서 주요 화면을 직접 확인한다.

## DB와 운영

- 운영 DB에 `pnpm db:push`를 실행하지 않는다.
- 표 변경은 승인과 백업 후 `pnpm exec drizzle-kit migrate`만 사용한다.
- 데이터 삭제와 일괄 파일 작업은 대상 확인, 백업, 승인 순서를 지킨다.
- 배포는 사용자가 명시적으로 요청할 때만 한 곳에서 진행한다.
- 운영 포트, 업로드 경로, 서버 설정을 임의로 바꾸지 않는다.

## 작업 종료와 다른 PC 인계

1. 변경 파일과 검사 결과를 다시 확인한다.
2. 수정한 파일만 커밋한다. `git add .`로 임시 파일까지 묶지 않는다.
3. 작업 브랜치를 GitHub에 push하고 PR을 만든다.
4. PR에 변경 내용, 확인 방법, DB 영향, 배포 여부, 남은 일을 적는다.
5. 승인 후 `main`에 병합하며, 다른 PC는 `git pull --ff-only origin main`으로 받는다.
6. 아직 병합 전이면 다른 PC에서 같은 원격 작업 브랜치를 받아 이어간다.

쉽게 낡는 비밀값이나 운영 접속정보는 인계 문서에 적지 않는다.
