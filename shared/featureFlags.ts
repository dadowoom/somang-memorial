/**
 * 추도일 알림 신청을 받을지 (2026-09-16 사용자 결정: 당분간 받지 않음).
 *
 * 2026-09-23 부터 문자 대신 카카오 알림톡(알리고)으로 보낸다. 카카오 템플릿 검수,
 * 알리고 포인트 충전, 관리자 시험 발송이 끝나면 true 로 바꾼다.
 * 절차: docs/REMINDER_ALIMTALK.md
 *
 * 화면(홈페이지·키오스크)과 서버가 같은 값을 본다 (2026-09-18). "알림 그만 받기"는
 * 이 값과 관계없이 언제나 된다.
 */
export const MEMORIAL_REMINDER_SIGNUP_ENABLED = false;
