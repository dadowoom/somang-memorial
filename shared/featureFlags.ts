/**
 * 추도일 알림 신청을 받을지 (2026-09-16 사용자 결정: 당분간 받지 않음).
 *
 * 문자 발신번호(솔라피) 등록이 아직이라, 신청을 받아도 알림 문자가 나가지 않는다.
 * 발신번호 등록을 마치면 true 로 바꾼다. 관리자 화면의 신청 목록은 그대로 둔다.
 *
 * 화면(홈페이지·키오스크)과 서버가 같은 값을 본다 (2026-09-18). 전에는 화면만
 * 숨기고 서버의 신청 통로는 열려 있어서, 밖에서 직접 부르면 아무 번호로나
 * 확인 문자가 나갔다.
 */
export const MEMORIAL_REMINDER_SIGNUP_ENABLED = false;
